# ScAnRoIdS
ScAnRoIdS: A Python-based automation layer for Nmap. This project wraps standard network scanning functionality into a Pythonic interface, making it easier to run, parse, and log network reconnaissance tasks without manual command-line overhead.
